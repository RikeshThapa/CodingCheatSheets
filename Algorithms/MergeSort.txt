#Pseudo code for Mergesort
# C= output attay [length= n]
# A = 1st sorted array [n/2]
# B = 2nd sorted array [n/2]
# i = 1
# j = 1
 
'''For k=1 to n
	if A(i) < B(j):
		C(k) = A(i)
		i++
	else [B(j)<A(i)]:
		C(k) = B(j)
		j++
   end
'''

#end cases, keep tracck of when you fall off A or B